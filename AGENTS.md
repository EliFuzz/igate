# Instructions

## Development Environment Management

Shell scripts for managing the development environment are available under `./scripts` directory that should be executed from the project root:

- `./scripts/validate`: must be executed as the last check once the code changes finished to validate that everything is working as expected to complete the task successfully.
